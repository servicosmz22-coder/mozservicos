import { Bell, BriefcaseBusiness, Home, MessageCircle, Plus, Search, User } from 'lucide-react';
import { Link, useLocation } from '@tanstack/react-router';

const items=[['/feed','Feed',Home],['/discover','Descobrir',Search],['/jobs','Pedidos',BriefcaseBusiness],['/profile','Perfil',User]] as const;
export function AppShell({children}:{children:React.ReactNode}){const loc=useLocation();return <div className="min-h-screen bg-[#F8F9FA] pb-20"><header className="sticky top-0 z-40 border-b bg-white/95 backdrop-blur"><div className="mx-auto flex h-16 max-w-2xl items-center justify-between px-4"><Link to="/feed" className="text-xl font-extrabold tracking-tight text-[#00A651]">Serviço<span className="text-[#FCB514]">Moz</span></Link><div className="flex items-center gap-1"><Link to="/notifications" className="relative grid size-10 place-items-center rounded-full hover:bg-gray-100"><Bell size={21}/><span className="absolute right-1 top-1 size-2 rounded-full bg-red-500"/></Link><Link to="/messages" className="relative grid size-10 place-items-center rounded-full hover:bg-gray-100"><MessageCircle size={21}/></Link></div></div></header><main className="mx-auto max-w-2xl">{children}</main><nav className="fixed inset-x-0 bottom-0 z-50 border-t bg-white"><div className="mx-auto grid h-16 max-w-2xl grid-cols-5 items-center px-2">{items.slice(0,2).map(([to,label,Icon])=><Nav key={to} to={to} label={label} active={loc.pathname===to} Icon={Icon}/>)}<Link to="/create" className="relative -top-5 mx-auto grid size-14 place-items-center rounded-full border-4 border-[#F8F9FA] bg-[#00A651] text-white shadow-lg"><Plus size={28}/></Link>{items.slice(2).map(([to,label,Icon])=><Nav key={to} to={to} label={label} active={loc.pathname===to} Icon={Icon}/>)}</div></nav></div>}
function Nav({to,label,Icon,active}:{to:string;label:string;Icon:any;active:boolean}){return <Link to={to} className={`flex flex-col items-center gap-0.5 text-[11px] font-semibold ${active?'text-[#00A651]':'text-gray-400'}`}><Icon size={20}/><span>{label}</span></Link>}
